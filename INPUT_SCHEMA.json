{
  "title": "CheerioCrawler Template",
  "description": "lorem ipsum",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "URLs to start with.",
      "editor": "json",
      "prefill": [
        {
          "url": "https://www.example.com/category/15",
          "userData": { "handlerType": "handleStart", "storeName": "exampleStore" }
        }
      ]
    },
    "defaultCookies": {
      "title": "Cookies to set",
      "type": "object",
      "description": "Cookies to set on each request",
      "editor": "json"
    }
  },
  "required": [
    "startUrls"
  ]
}
